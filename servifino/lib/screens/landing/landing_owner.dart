import 'package:flutter/material.dart';

class LandingOwner extends StatelessWidget {
  const LandingOwner({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Owner page'),
      ),
    );
  }
}
